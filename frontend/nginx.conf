server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA-Fallback: alle Routen liefern index.html aus
    # Traefik übernimmt das /api/*-Routing zum Backend-Container
    location / {
        try_files $uri $uri/ /index.html;
    }
}