"""GET /calendars - Liste aller (synchronisierten) Kalender.

Wird in Phase 2 implementiert.
"""

# Phase 2:
#   from fastapi import APIRouter, Depends
#   router = APIRouter(prefix="/calendars", tags=["calendars"])
#
#   @router.get("")
#   def list_calendars(db: Session = Depends(get_db)) -> list[CalendarOut]: ...
